const signup=document.getElementById("signup");signup.addEventListener("submit",(e=>{e.preventDefault();let s=signup.email.value,t=signup.password.value;t===signup.cpassword.value?firebase.auth().createUserWithEmailAndPassword(s,t).then((()=>{alert("Sign up successful,redirecting to login page..."),window.location.assign("index.html")})).catch((e=>alert(e.message))):alert("Passwords do not match!")}));